<form method="post" action="{{ path('gagnats_delete', {'idGagnant': gagnant.idGagnant}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ gagnant.idGagnant) }}">
    <button class="btn">Delete</button>
</form>
