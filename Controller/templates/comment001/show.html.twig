{% extends 'base.html.twig' %}

{% block title %}Comment{% endblock %}

{% block body %}
    <h1>Comment</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ comment.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('comment001_index') }}">back to list</a>

    <a href="{{ path('comment001_edit', {'id': comment.id}) }}">edit</a>

    {{ include('comment001/_delete_form.html.twig') }}
{% endblock %}
